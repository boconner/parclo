# Mapbox public token (required for the store map)
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# Backend API base URL (set this to your Railway backend service URL in production)
# Leave empty for local dev — the Vite dev proxy handles /api automatically
VITE_API_URL=
